decode = list(input())
code = list(input())
c = {}
for i in range(len(code)):
    c[code[i]] = decode[i]

want = list(input())
ans = ''
for i in want:
    if i in c:
        ans+=c[i]
    else:
        ans+='.'
print(ans)
